$("#applicants").html("<%= escape_javascript(render("applicants")) %>");
$('#saving_modal').modal({
  keyboard: false,
  backdrop: 'static',
  show: false
});
$('#saving_modal').modal('hide');
$("td[rel~=tooltip]").tooltip({ trigger: 'hover' });
